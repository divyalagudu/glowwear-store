async function guard(){if(!token()||user()?.role!=="admin"){location.href="login.html";return}loadAdmin()}async function loadAdmin(){try{let d=await api("/admin/dashboard");stats.innerHTML=`<div class="stat">Products<b>${d.products}</b></div><div class="stat">Orders<b>${d.orders}</b></div><div class="stat">Revenue<b>₹${d.revenue.toLocaleString("en-IN")}</b></div>`;let p=await api("/products?limit=100");adminProducts.innerHTML=p.products.map(x=>`<div class="admin-item"><span>${x.name} — ₹${x.price} — Stock ${x.stock}</span><button onclick="deleteProduct('${x._id}')">Delete</button></div>`).join("");let o=await api("/admin/orders");adminOrders.innerHTML=o.orders.map(x=>`<div class="admin-item"><span>#${x._id} — ₹${x.total} — ${x.status}</span><select onchange="status('${x._id}',this.value)"><option>${x.status}</option><option>confirmed</option><option>packed</option><option>shipped</option><option>delivered</option><option>cancelled</option></select></div>`).join("")}catch(e){document.body.innerHTML+="<p>"+e.message+"</p>"}}productForm.onsubmit=async e=>{e.preventDefault();try{await api("/products",{method:"POST",body:JSON.stringify({name:pname.value,category:pcat.value,price:Number(pprice.value),image:pimage.value,stock:Number(pstock.value)})});productForm.reset();loadAdmin()}catch(e){alert(e.message)}};async function deleteProduct(id){if(confirm("Delete product?")){await api("/products/"+id,{method:"DELETE"});loadAdmin()}}async function status(id,value){await api("/admin/orders/"+id,{method:"PATCH",body:JSON.stringify({status:value})});loadAdmin()}guard()